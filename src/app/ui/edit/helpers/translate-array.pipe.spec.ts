import { TranslateArrayPipe } from './translate-array.pipe';

describe('TranslateArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
